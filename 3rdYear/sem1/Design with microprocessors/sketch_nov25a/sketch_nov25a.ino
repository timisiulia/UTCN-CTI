iuliatimis
#7226

FredBoat♪♪
BOT
 — 05.01.2022
You must first be given a selection to choose from.
Guzar — 05.01.2022
;;s
FredBoat♪♪
BOT
 — 05.01.2022
Skipped track #1: Tzanca Uraganu - Iubirea dupa cearta [videoclip oficial] 2021
Guzar — 05.01.2022
;;p mafia mafia
FredBoat♪♪
BOT
 — 05.01.2022
Song #1 has been selected: Mohamed Ramadan - Mafia ( Music Video ) / محمد رمضان - مافيا (04:35) 
Guzar — 05.01.2022
library IEEE;
use IEEE.STD_LOGIC_1164.ALL;

entity DCDI is
    Port (
            instr: in std_logic_vector(31 downto 0);
Extinde
message.txt
3 KB
;;p dj magic romania sistemul percutiilor
FredBoat♪♪
BOT
 — 05.01.2022
Song #1 has been selected: Sistemul Percutiilor Moombahton ❌ Dj Magic Ultra Mix (03:02) 
Guzar — 05.01.2022
;;p dj magic romania
FredBoat♪♪
BOT
 — 05.01.2022
Song #3 has been selected: Dj Magic ❌ Tdr - Sistemul Balansat ❌Instrumental Ultra Mix (03:20) 
andreiv — 05.01.2022
signal MD : MD_TYPE := (others=>(others=>'0'));
Guzar — 05.01.2022
;;p dj magic vai cum se misca
FredBoat♪♪
BOT
 — 05.01.2022
Song #1 has been selected: Dj Magic feat. TDR - Vai Cum Se Misca ❌ Reggaeton ❌ Ultra Mix (03:27) 
Guzar — 05.01.2022
;;p te cunosc dupa sandale
FredBoat♪♪
BOT
 — 05.01.2022
Song #1 has been selected: NIK LUPU - TE CUNOSC DUPA SANDALE, LIVE, ZOOM STUDIO (05:08) 
andreiv — 05.01.2022
import org.junit.jupiter.api.Test;
import org.assertj.core.api.Assertions;
<!-- https://mvnrepository.com/artifact/org.assertj/assertj-core -->
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.8.0</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.12</version>
            <scope>test</scope>
        </dependency>
Guzar — 05.01.2022
;;p cu lumina stinsa
FredBoat♪♪
BOT
 — 05.01.2022
Song #1 has been selected: Tzanca Uraganu ❌ Cu lumina stinsa HIT ❌ (03:21) 
FredBoat♪♪
BOT
 — 05.01.2022
All users have left the voice channel. The player has been paused.
andreiv — 09.01.2022
Imagine
andreiv — 10.01.2022
vec4 texColorDiffuse = texture(diffuseTexture, fTexCoords);
    if(texColorDiffuse.a < 0.1 ){
        discard;
    }
    vec4 texColorSpecular = texture(specularTexture, fTexCoords);
    if(texColorSpecular.a < 0.1 ){
        discard;
    }
    vec3 color = min((ambient + diffuse) * texColorDiffuse.rgb + specular * texColorSpecular.rgb, 1.0f);
andreiv — 10.01.2022
faces.push_back("models/skybox/right.tga");
    faces.push_back("models/skybox/left.tga");
    faces.push_back("models/skybox/top.tga");
    faces.push_back("models/skybox/bottom.tga");
    faces.push_back("models/skybox/back.tga");
    faces.push_back("models/skybox/front.tga");
mySkyBox.Load(faces);
skyboxShader.loadShader("shaders/skyboxShader.vert", "shaders/skyboxShader.frag");
mySkyBox.Draw(skyboxShader, view, projection);
std::vector<const GLchar*> faces;
Guzar — 10.01.2022
;;p stau ca vulturul pe stanca
FredBoat♪♪
BOT
 — 10.01.2022
Song #1 has been selected: Sorinel Pustiu - Stau ca Vulturu Pe Stanca (03:49)

🆕 You can also view and control the player at https://dashboard.fredboat.com/music-player/908043735911251968 
Guzar — 10.01.2022
;;p1
Guzar — 10.01.2022
;;p ionel tudorache portofele
FredBoat♪♪
BOT
 — 10.01.2022
Song #1 has been selected: Ionel Tudorache - Portofele (@Politică şi delicateţuri) (03:12) 
Guzar — 10.01.2022
;;p te cunosc dupa sandale
FredBoat♪♪
BOT
 — 10.01.2022
Song #1 has been selected: NIK LUPU - TE CUNOSC DUPA SANDALE, LIVE, ZOOM STUDIO (05:08) 
andreiv — 10.01.2022
lightRotation = glm::rotate(glm::mat4(1.0f), glm::radians(angle), glm::vec3(0.0f, 1.0f, 0.0f));
        glUniform3fv(lightDirLoc, 1, glm::value_ptr(glm::inverseTranspose(glm::mat3(view * lightRotation)) * lightDir));
andreiv — 10.01.2022
glm::mat4 lightView = glm::lookAt(glm::inverseTranspose(glm::mat3(view * lightRotation)) * lightDir, glm::vec3(0.0f), glm::vec3(0.0f, 1.0f, 0.0f));
FredBoat♪♪
BOT
 — 10.01.2022
All users have left the voice channel. The player has been paused.
andreiv — 10.01.2022
if (key == GLFW_KEY_M && action == GLFW_PRESS)
        showDepthMap = !showDepthMap;
octopus248 — 10.01.2022
https://we.tl/t-CDMJTzOjB7
MAPA2.rar
1 file sent via WeTransfer, the simplest way to send your files around the world
Imagine
andreiv — ieri la 21:31
Structuri de date 

    Printre singurele structuri de date utilizate este cea din clasa de camera unde avem nevoie de mai multi vectori, cum ar fi pozitia camerei, directia de vizualizare, dar si coordonata de pozitie a camerei. Alte structuri de date importante sunt Glint in care sunt stocate adresele variabilelor uniform din shadere, dar si vectori de diferite dimensiuni si marimi. Nu in ultimul rand am folosit data de tipul Model sau Shaders dar si tipuri normal din C++ cum ar fi bool, int, char. Variabilele normale au fost utilizate pentru a putea implementa diverse functionalitati.

Ierarhii de clase

    In afara de main fiecare clasa are cate un header. Urmeaza acum sa dau exemplu de clase si fiecare ce face. Clasa de camera are implementat in ea camera, clasa de window are imlementate toate functionalitatile pentru creearea si stergerea unei ferestre de desenare, clasa de skyBox incarca, deseneaza si efectueaza diversee operatii pe sky box, clasa de mesh deseneaza obiecte, clasa de shaders fefectueaza toate operatiile necesare implementarii tuturor functionalitatilor unui shader nu in ultimul rand std_image si tiny_obj_loader textureaza respectiv incarca un ubiect in OpenGl.
octopus248 — ieri la 21:36
;;p canta muzica
FredBoat♪♪
BOT
 — ieri la 21:36
Song #1 has been selected: Oana Radu - Cand am o zi grea (Piesa Revelionului 2022) 🎆 (03:03)

🆕 You can also view and control the player at https://dashboard.fredboat.com/music-player/908043735911251968 
octopus248 — ieri la 21:47
if (pressedKeys[GLFW_KEY_F])
    {
        glPolygonMode(GL_FRONT_AND_BACK,GL_LINE);
    }

    if (pressedKeys[GLFW_KEY_G])
    {
        glPolygonMode(GL_FRONT_AND_BACK, GL_POINT);
    }

    if (pressedKeys[GLFW_KEY_H])
    {
        glPolygonMode(GL_FRONT_AND_BACK, GL_FILL);
    }
octopus248 — ieri la 22:40
https://we.tl/t-alXnjSoTE8
OpenGL Project Core 2022-01-12 22-31-24.rar
1 file sent via WeTransfer, the simplest way to send your files around the world
Imagine
FredBoat♪♪
BOT
 — ieri la 22:41
All users have left the voice channel. The player has been paused.
andreiv — azi la 0:49
Tipul fișierului atașat: archive
DP.rar
10.78 KB
Tipul fișierului atașat: acrobat
DP-Viman_Andrei_Liviu-30236.pdf
151.79 KB
Tipul fișierului atașat: acrobat
SE-GoF.pdf
142.27 KB
andreiv — azi la 2:16
#include <FastLED.h>
#include <TimerOne.h>
#define NUM_LEDS 12
#define DATA_PIN 6
#define MIC_PIN 2
#define OFFSET 900
int btn1 = 8, btn2 = 9, btn3 = 10, btn4 = 11;
int nn = 0;
int citire = 0;
int b11;
int b22;
int b33;
int b44;


CRGB leds[NUM_LEDS];

void setup() {
  Serial.begin(9600);
  pinMode(MIC_PIN, INPUT);
  pinMode(btn1, INPUT_PULLUP);
  pinMode(btn2, INPUT_PULLUP);
  pinMode(btn3, INPUT_PULLUP);
  pinMode(btn4, INPUT_PULLUP);
  FastLED.addLeds<NEOPIXEL, DATA_PIN>(leds, NUM_LEDS);
  Timer1.initialize(15000);
  Timer1.attachInterrupt(citireSenzori); 


}

void citireSenzori(void)
{
  citire = 1;
}

int transformare() {
  int sum = 0;
  for ( int i = 0; i < 1023; i++) {
    sum += digitalRead(MIC_PIN);

  }
  return 1023 - sum;
}

void curcubeuCuNumarLeduri(int valCitita) {
  int numLedsToLight = map(valCitita, 0, OFFSET, 0, NUM_LEDS);

  // First, clear the existing led values
  FastLED.clear();
  for (int led = 0; led < numLedsToLight; led++) {
    int color = map(rand(), 0, 1000, 0, 255);
    leds[led].setHue( color );
    FastLED.setBrightness(50);
  }
  FastLED.show();

  delay(10);
}

void dimLeduriRed(int valCitita) {

  // First, clear the existing led values
  FastLED.clear();
  for (int led = 0; led < NUM_LEDS; led++) {
    leds[led].r = 255;
    leds[led].g = 10;
    leds[led].b = 10;
  }
  int val_afis = map(valCitita, 0, OFFSET, 0, 150);
  FastLED.setBrightness(val_afis);
  FastLED.show();

  delay(10);

}

void deLaMijloc(int valCitita)  {
  int numLedsToLight = map(valCitita, 0, OFFSET / 2, 0, 6);

  // First, clear the existing led values
  FastLED.clear();
  for (int led = 0; led < numLedsToLight; led++) {
    int color = map(rand(), 0, 1000, 0, 255);
    leds[led].setHue( color );
    leds[12 - led].setHue( color );
    FastLED.setBrightness(50);
  }
  FastLED.show();

  delay(10);

}



void schimbaCulori(int valCitita) {
  int numLedsToLight = map(valCitita, 0, OFFSET / 2, 0, 6);
  for (int led = 0; led < numLedsToLight; led++) {
    int color = map(rand(), 0, 1000, 0, 255);
... (43 rânduri rămase)
Restrânge
message.txt
3 KB
﻿
#include <FastLED.h>
#include <TimerOne.h>
#define NUM_LEDS 12
#define DATA_PIN 6
#define MIC_PIN 2
#define OFFSET 900
int btn1 = 8, btn2 = 9, btn3 = 10, btn4 = 11;
int nn = 0;
int citire = 0;
int b11;
int b22;
int b33;
int b44;


CRGB leds[NUM_LEDS];

void setup() {
  Serial.begin(9600);
  pinMode(MIC_PIN, INPUT);
  pinMode(btn1, INPUT_PULLUP);
  pinMode(btn2, INPUT_PULLUP);
  pinMode(btn3, INPUT_PULLUP);
  pinMode(btn4, INPUT_PULLUP);
  FastLED.addLeds<NEOPIXEL, DATA_PIN>(leds, NUM_LEDS);
  Timer1.initialize(15000);
  Timer1.attachInterrupt(citireSenzori); 


}

void citireSenzori(void)
{
  citire = 1;
}

int transformare() {
  int sum = 0;
  for ( int i = 0; i < 1023; i++) {
    sum += digitalRead(MIC_PIN);

  }
  return 1023 - sum;
}

void curcubeuCuNumarLeduri(int valCitita) {
  int numLedsToLight = map(valCitita, 0, OFFSET, 0, NUM_LEDS);

  // First, clear the existing led values
  FastLED.clear();
  for (int led = 0; led < numLedsToLight; led++) {
    int color = map(rand(), 0, 1000, 0, 255);
    leds[led].setHue( color );
    FastLED.setBrightness(50);
  }
  FastLED.show();

  delay(10);
}

void dimLeduriRed(int valCitita) {

  // First, clear the existing led values
  FastLED.clear();
  for (int led = 0; led < NUM_LEDS; led++) {
    leds[led].r = 255;
    leds[led].g = 10;
    leds[led].b = 10;
  }
  int val_afis = map(valCitita, 0, OFFSET, 0, 150);
  FastLED.setBrightness(val_afis);
  FastLED.show();

  delay(10);

}

void deLaMijloc(int valCitita)  {
  int numLedsToLight = map(valCitita, 0, OFFSET / 2, 0, 6);

  // First, clear the existing led values
  FastLED.clear();
  for (int led = 0; led < numLedsToLight; led++) {
    int color = map(rand(), 0, 1000, 0, 255);
    leds[led].setHue( color );
    leds[12 - led].setHue( color );
    FastLED.setBrightness(50);
  }
  FastLED.show();

  delay(10);

}



void schimbaCulori(int valCitita) {
  int numLedsToLight = map(valCitita, 0, OFFSET / 2, 0, 6);
  for (int led = 0; led < numLedsToLight; led++) {
    int color = map(rand(), 0, 1000, 0, 255);
    leds[led].setHue( color );
    leds[12 - led].setHue( color );
    FastLED.setBrightness(50);
  }
  FastLED.show();

  delay(10);

}


void loop() {
  int valoare = transformare();
  if (nn == 0)  schimbaCulori(valoare);
  if (nn  == 1) dimLeduriRed(valoare);
  if (nn  == 2) deLaMijloc(valoare);
  if (nn  == 3) curcubeuCuNumarLeduri(valoare);

  // variabile pentru starea butoanelor
  if (citire == 1) {
    citire = 0;
    b11 = digitalRead(btn1);
    b22 = digitalRead(btn2);
    b33 = digitalRead(btn3);
    b44 = digitalRead(btn4);
  }


  if (b11 == LOW) {
    nn = 0;
  }
  if ( b22 == LOW) {
    nn = 1;
  }
  if (b33 == LOW) {
    nn = 2;
  }

  if (b44 == LOW) {
    nn = 3;
  }

}
